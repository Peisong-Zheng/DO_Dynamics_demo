# VS Code AI 代理提示词：两只水桶的突变实验台

将下方“提示词开始”至“提示词结束”的全部内容交给 VS Code 中的 AI 编程代理。代理应在你已经打开的当前工作区完成项目；不需要另外提供对话记录、PDF 或附件。

---

## 提示词开始

你是一名擅长科学可视化、Three.js 和交互教学的前端工程师。请直接在当前工作区创建并完成一个可运行、可构建、可静态部署的中文网站。先检查当前目录及适用的项目说明；如果目录已有内容，保留已有工作并合理接入。不要再创建一层同名项目目录。除非存在真实阻碍，否则自行作出常规实现选择，完成代码、验证和 README，不要只交付方案或代码片段。

项目名称：**两只水桶的突变实验台**。

网站通过两只透明自动排水桶的 Three.js 动画，帮助用户理解：

1. 自维持的慢 - 快振荡：固定背景、没有随机扰动，系统仍反复积累和释放。
2. 噪声触发的可激发过程：固定背景、关闭扰动后，系统趋向门槛以下的稳定状态；有效扰动可以启动一次快速释放，随后返回。
3. 外部背景改变平均输入或门槛，可以调整事件时刻，也可以把同一套装置从一种动力学状态变成另一种。

最终由我把代码上传 GitHub，再用 Cloudflare Pages 的 Git 集成托管。你负责在工作区完成网站及静态部署说明，保留发布操作给我执行。

### 一、先保证例子的逻辑成立

两边必须是**同一种装置、同一套方程、同一个状态机**。左右的区别来自参数。不要给左边写一个循环播放动画，再给右边写一个随机计时播放动画。

装置由以下部分组成：

- 一个透明、直立、等截面的水桶或水箱。
- 上方持续进水的管道；进水可以恒定，也可以随机忽强忽弱。
- 底部一只始终开放的小漏水口，持续缓慢排水。
- 另一只可自动打开的大排水阀。
- 一条可调的高门槛线 H，以及一条固定且较低的复位线 h_r。
- 水位达到 H 时，大阀打开；下降到 h_r 时，大阀关闭，然后继续蓄水。

可以用浮子、连杆和可移动限位件暗示自动开关机制，但这是教学装置，不需要求解真实机械、流体或虹吸。使用自动排水阀而不是倾倒整个桶，以便让进水、排水和水位变化始终一致。

用户应一眼看到：水位、门槛、进水、小口漏水、大阀开关；不能依靠打开公式说明才看懂场景。

### 二、技术方案和交付范围

- 使用 Vite + TypeScript + Three.js + 普通 HTML/CSS；这个项目无需 React 或后端。
- 安装与当前环境兼容的稳定依赖，提交 lockfile，保证 `npm ci` 可重复安装。不要依赖运行时 CDN、远程模型、远程贴图、API 或登录服务。
- Three.js 资源用基础几何体和程序化材质生成。水面可以做轻微波纹，但不需要真正的流体模拟。
- 生产构建生成 `dist/`，里面只包含静态资源。开发、构建、预览命令分别为 `npm run dev`、`npm run build`、`npm run preview`。
- 不需要 SSR、Cloudflare Functions、数据库、Cloudflare Vite 服务端插件或服务端密钥。
- 本地通过开发服务器或构建预览运行；不要承诺双击源码 `index.html` 就能正常加载 ES modules。
- 单页设计，不做需要服务器回退的多页路由。
- 核心模拟不能依赖 Three.js，必须可以在无 DOM 环境单独验证。

建议结构，可按实际需要精简：

```text
index.html
package.json
package-lock.json
tsconfig.json
vite.config.ts
README.md
src/
  main.ts
  styles.css
  simulation/
    types.ts
    engine.ts
    random.ts
    presets.ts
  scene/
    createScene.ts
    tank.ts
    water.ts
  ui/
    controls.ts
    charts.ts
    copy.ts
tests/
  simulation.test.ts
```

### 三、必须实现的教学模型

所有数值是教学用相对单位；模拟时间以秒计，不映射为真实气候年份。

状态变量：

- `h`：归一化水量。桶的横截面积视为常数，因此也代表水位。
- `phase`：`FILL` 或 `DRAIN`，分别表示大阀关闭和打开。
- `r`：平均进水量。
- `k`：小漏水口的线性排水系数。
- `kappa`：大阀额外排水系数。
- `H`：高门槛。
- `hReset`：低复位水位。
- `a`：随机进水的相对波动强度。
- `z`：共享的有时间相关性的随机过程。

方程：

```text
q(t) = r * [1 + a * tanh(z(t))]
dh/dt = q(t) - k*h - I_drain*kappa*h
```

其中，大阀关闭时 `I_drain=0`，打开时为 `1`。

高低阈值状态机：

```text
FILL：从下方首次达到 H -> DRAIN，并登记一次事件。
DRAIN：降到 hReset -> FILL。
```

这是装置内置的自动反馈，不是每轮有人按开关。

必须保证：

- 达到高门槛是排水原因，动画只能反映该状态转换。
- 大阀打开后，即使水位马上低于 H，也必须继续排到 hReset 才关闭，避免高门槛附近抖动。
- 排水过程中仍持续进水，小漏口也仍排水。
- 复位时不能瞬间把水量置零；用方程把水排到低复位线。
- 水位为非负，不能通过随意截断水位掩盖数值错误。
- 当用户把 H 降到当前 FILL 水位以下，下一物理步应按条件开始排水。
- 修改 H 不应让已经打开的大阀提前关闭。

#### 默认参数

| 参数 | A 桶：初始可自行循环 | B 桶：初始依赖扰动 |
|---|---:|---:|
| r | 0.240 | 0.144 |
| k | 0.16 | 0.16 |
| kappa | 8.0 | 8.0 |
| H | 1.00 | 1.00 |
| hReset | 0.12 | 0.12 |
| a | 0.00 | 0.90 |
| 初始 h | 0.70 | 0.70 |

共同参数：噪声相关时间 `tau=1.2`，物理步长 `dt=1/120`，默认随机种子 `20260914`。

初始水位一致，并且已部分蓄水，以缩短首次观察所需时间。重置时清楚恢复到该初始水位；日常开关噪声、调整参数时不得偷偷重置。

无噪声、大阀关闭时，假想平衡水位为：

```text
hEq = r / k
```

它表示“如果大阀一直不打开，水位最终会趋向哪里”，不等于发生排水时的实际平均水位。

- A 桶：hEq=1.50，高于 H=1.00，因此能自行触发并反复循环。
- B 桶：hEq=0.90，低于 H=1.00，因此关闭噪声后不会自行跨越门槛。

在允许的参数范围内，应始终满足 `qMax/(k+kappa) < hReset`，使大阀打开后能够完成排水和复位。

### 四、随机过程：不能直接随机安排“事件”

使用 OU 有色噪声，稳态分布为均值 0、方差 1 的正态分布：

```text
dz = -(z/tau) dt + sqrt(2/tau) dW
```

每个固定物理步采用其精确离散更新：

```text
b = exp(-dt/tau)
zNext = b*z + sqrt(1 - b*b) * normal01()
```

初始化 z 时也从标准正态抽样。使用以下可复现生成方式；Box-Muller 每次调用消耗两个均匀随机数，并只返回 cosine 分量，不缓存 sine 分量：

```ts
function mulberry32(seed: number) {
  return () => {
    let t = seed += 0x6D2B79F5;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function normal01(random: () => number) {
  const u1 = Math.max(random(), 1e-12);
  const u2 = random();
  return Math.sqrt(-2 * Math.log(u1)) * Math.cos(2 * Math.PI * u2);
}
```

每个物理步只生成一次全局 z，两个桶读取同一个 z，但各自使用自己的 r 和 a。它们接受的是同一段**相对流量波动**；平均流量不同，所以绝对流量变化不一定相同，文案不要说成完全相同的绝对输入。

- 关闭某桶噪声就是令该桶 a=0。全局随机过程仍按模拟时间推进。
- 粒子、水花、装饰纹理不能消耗模拟 PRNG；另用独立随机源，或完全确定的装饰。
- 同一 seed、同一初态、同一参数和同一模拟时刻的操作序列，应重现相同轨迹。
- 界面帧率、图表是否显示、窗口大小不得改变随机数的消耗顺序。
- `0 <= a <= 0.95`，所以进水始终为正，不出现负流量。
- 对称稳态分布使长期平均流量仍为 r；有限时间内的样本平均当然会有波动。不能把噪声实现成只加水、不减弱进水的持续正偏输入。

默认 seed 只是一条可复现的展示轨迹。对上述参数的轻量校准中，B 桶第一次事件约在第 14 秒，随后事件间隔不规则；实现后重新核验，不要将这些时间硬编码成排水触发器，也不要将其当成真实数据或普遍统计规律。

特别保留这个正确行为：如果从门槛以下出发，且 `r*(1+a)/k <= H`，当前有界噪声即使持续到最强也不能使其在有限时间内跨过 H。不要为了“好看”偷偷强制触发。

### 五、数值步进、时间和水量收支

1. 使用固定物理步长和 accumulator，将模拟与 `requestAnimationFrame` 解耦。显示速度只改变每秒推进多少模拟时间。
2. 页面隐藏时暂停推进，并在恢复时清除多余的墙钟时间累积；不要回来时突然补算几分钟。极慢设备可以让动画变慢，但不能改用任意大 dt 跳过阈值。
3. 每个物理步内，把当前 q、H 等参数视为常数。H/r 的自动渐变也在固定物理步上更新。
4. 令 `lambda=k+(phase==='DRAIN' ? kappa : 0)`，采用分段精确解：

```text
hs = q/lambda
hNext = hs + (h - hs) * exp(-lambda*delta)
```

5. 如果一步内穿越高门槛或复位线，解出穿越时间，先积分到边界，切换状态，再用新状态积分剩余时间。

```text
tCross = -log((hTarget - hs)/(hStart - hs))/lambda
```

仅在运动方向正确、分式合法、`0 <= tCross <= remainingDt` 时使用该解；显式处理从边界开始、阈值被移动到水位以下、人工加水造成越界等情况。

6. 无噪声且 `hEq===H` 时，从下方出发只会渐近接近 H，不能由于浮点舍入生成假事件。上涨穿越的判定要结合该分段平衡值确实位于 H 之上；边界邻近值使用有依据的容差，不能把容差当作提前触发距离。
7. 在引擎中累计进水、慢漏水、大阀排水和人工加水，用于守恒验证。对于上述指数解：

```text
integralH = hs*delta + (hStart-hs)*(1-exp(-lambda*delta))/lambda
inputVolume = q*delta
slowLeakVolume = k*integralH
fastDrainVolume = isDraining ? kappa*integralH : 0
```

8. 不需要在主界面展示守恒调试表，但验证时应满足初始水量加输入减输出等于当前水量。
9. 事件日志只记录真实的 FILL -> DRAIN 转换及当时参数；不要在每个排水帧重复计数。

### 六、页面布局与视觉设计

设计成安静、清楚、有质感的教学实验台，不做控制台或数据大屏。

**首屏：**

- 标题：“两只水桶的突变实验台”。
- 一句主说明：“同一种装置，为什么一只会自己循环，另一只需要扰动？”
- 立刻出现两个可比较的 3D 桶和实际动画，页面不依赖鼠标悬停才显示核心内容。
- A/B 固定标识保留；“可自行循环 / 依赖扰动”等标签必须根据当前参数动态变化，不能固定绑定左右位置。

**3D 场景：**

- 桌面端两桶并排，移动端上下排列；推荐单个 WebGLRenderer、同一个场景，根据宽度调整物体位置和相机，避免多个 WebGL 上下文。
- 采用清晰的正交或轻透视视角；默认相机就能看到水位、两只出口和阀门。无需用户学习拖动视角。
- 桶可以用开放前面的剖面结构，避免透明排序使水面消失。水面视觉高度严格跟随 h，尽量用几何切面解决透明问题。
- 细水流始终存在；大阀打开时出现明显粗水流、阀门动作和短暂水花。进水水柱强弱反映 q(t)。水流装饰可以简化，但必须与模拟方向和状态一致。
- 给两种排水路径加短标签：“持续慢漏”“门槛触发快排”。
- H 用醒目的水平虚线表示；hReset 用较弱的线表示；hEq 用另一种线型表示，并标为“无波动平衡水位”。颜色不能是唯一编码。
- 调整 H 时，门槛线和浮子限位件同时移动。调整 r 时，进水管变化且 hEq 线移动。
- 几何容量及标记范围应覆盖所有控制允许的水位和 hEq；不要把超出显示范围的平衡水位悄悄截到桶顶。可以把 hEq 显示在桶旁独立标尺上。
- 不要做水绕竖轴无意义旋转、自动绕场景飞行、镜头抖动或持续发光脉冲。

**曲线：**

- 场景下面为每个桶提供一个紧凑的真实时间序列图，两图时间窗和纵轴尺度一致。
- 图中显示 h(t)、H(t)、hEq(t) 和事件标记；H/r 在历史中有变化时，要绘出当时的值，不能把整个历史都改成当前参数。
- 桶的水位、阀门状态、事件数量和曲线必须来自同一个引擎输出。
- 共享滚动时间窗，例如最近 60 个模拟秒；使用有上限的环形缓冲区，图表降采样与物理积分分离。
- 坐标清楚写“模拟时间（秒）”“水位（相对单位）”。线型、图例和 A/B 标识清晰，不能让用户猜两条线各对应什么。
- 图表可用 SVG 或 Canvas 2D；不必为简单曲线引入庞大图表库。

### 七、控制与引导实验

每桶提供三个容易操作的滑块，并显示数值：

- 平均进水 r：`0.06–0.30`，步长 `0.001`。
- 排水门槛 H：`0.55–1.65`，步长 `0.01`。
- 随机进水强度 a：`0–0.95`，步长 `0.01`；附开关，关闭时记住原强度以便恢复。

固定 k、kappa、hReset 和 tau，放在折叠的模型说明中，避免主界面堆满参数。

每桶增加一个按钮：“加一小杯水（+0.15）”。点击后真实增加 0.15 水量并计入输入，触发与否仍由模型判断。这是一次人为脉冲，必须在图上单独标记；不能把该按钮实现成无条件排水。加水不改变平均 r、噪声幅度或 H。

加水请求在下一固定物理步执行，每桶同时最多保留一个待执行请求；待执行时暂时禁用按钮。模拟暂停时也禁用加水按钮。规定可视水量上限为 2.00，若加水会超出该上限，则禁用并明确说明，不能偷偷截断水量。默认参数和控制范围中的自主演化不会达到这一容量限制。暂停时可以调整参数，但由这些调整引起的水量演化或阀门转换应在恢复后的物理步执行。

公共控制：

1. **暂停 / 继续**：暂停模拟时钟、噪声和所有物理状态。主按钮文案与实际状态一致。
2. **关闭全部噪声**：只将 a 设 0，保留两桶当前水位、阀门状态、历史和背景参数；不要重置。若某桶正在排水，让它继续完成。
3. **冻结背景参数**：只停止 H/r 的自动渐变，并保留冻结时的数值；继续进水、模拟时间和噪声。无自动渐变时可以显示“背景已固定”。这与暂停、关闭噪声不同。
4. **重置并重放**：清空历史、恢复明确的默认参数/初态和当前 seed，重现同一条轨迹。
5. **换一组随机波动**：生成新 seed，再从相同初态重跑；不要保留旧历史混在一起。seed 可以在折叠说明中查看。
6. **播放速度**：提供 `0.25× / 0.5× / 1× / 2×`，默认 `1×`。必须只改变时间推进速度，不改变模型参数。

提供四个可一键载入的引导实验。按钮统一写成“载入实验：……”，明确加载实验会重置水位和历史；这与上面的实时控制区别清楚。

**实验 A：无扰动对照**

- A/B 使用默认 r、H，均 a=0，h=0.70。
- 用户看到 A 自行循环，而 B 趋近 0.90 并停在门槛以下。
- 一句解释：“持续进水不意味着一定越过门槛；输入也可能先与漏水平衡。”

**实验 B：加入随机进水（默认首次打开页面）**

- 使用完整默认参数与默认 seed。
- B 有机会出现不规则排水，A 仍自行循环。
- 突出“关闭全部噪声”按钮，让用户做保留当前状态的反事实实验。
- 一句解释：“噪声可以负责启动，之后的排水与复位由装置自动完成。”

**实验 C：逐渐降低门槛**

- 从无扰动对照加载，r 保持默认。
- 初始背景保持 3 个模拟秒，之后仅将 B 的 H 在 10 个模拟秒内从 1.00 线性降至 0.75，然后维持不变。
- 渐变期间实时显示 H，用户可随时冻结背景。
- 事件仍由 h/H 关系触发，不能绑定到渐变动画结束。
- 一句解释：“外部背景可以让同一个系统从稳定状态变成自行循环。”

**实验 D：逐渐增加平均输入**

- 从无扰动对照加载，H 始终为 1.00。
- 初始保持 3 秒，之后仅将 B 的 r 在 10 秒内从 0.144 线性增至 0.200，然后维持不变。
- 显示 hEq 随输入升高，水位经过积累后自行到达门槛。
- 一句解释：“改变输入会改变积累过程，也可能改变系统能否自行越过门槛。”

人工滑动某个正在自动渐变的参数时，取消该参数的自动渐变，避免界面把用户刚调的值改回去。冻结、暂停、重置的语义必须在所有实验中一致。

### 八、动态状态文案与科学边界

把“眼下正在做什么”与“固定背景、无噪声时最终会怎样”分开显示：前者是蓄水/快排，后者是动力学状态。

根据当前 r、H、a 判断：

- `r/k > H`：可自行循环；开噪声后仍可写“可自行循环，时刻受扰动影响”。
- `r/k < H` 且 a=0：趋向稳定水位；若正处于一次排水中，说明完成后将趋于稳定。
- `r/k < H`，且 `r*(1+a)/k > H`：扰动可能触发，发生时刻不规则。不要承诺每一段短演示都必有事件。
- `r*(1+a)/k <= H`：当前波动范围不足以越过门槛，前提是起始水位在 H 以下且没有额外人工加水。
- `r/k` 接近 H：临界边界。精确相等时，无噪声水位从下方渐近接近门槛；在有噪声时另按扰动条件判断。

首屏下方保留简短、可折叠的解释，默认展开简要文字，公式放在进一步展开层：

> 两边是同一种装置。运行条件决定它能否自行循环，左边和右边的角色可以随着参数改变而交换。

> “自维持”不等于没有能量或物质输入。这里始终有平均进水；区别是每次事件是否需要新的有效扰动。

> 默认参数下，B 桶演示的是可激发过程：扰动启动一次快速释放，随后返回原有的稳定蓄水状态。它不是两个稳定状态之间的永久切换。

> 外部调控不是第三种互斥机制。它可以改变上述两类过程的门槛、推进速度和运行状态。

> 这里把快速、不规则的流量波动称为噪声，把平均进水和设定门槛称为背景条件。这是作用方式的区别，不意味着真实气候中的噪声一定来自气候系统外部。

> 这是教学模型；水位不是次表层温度的定量替代，也不能据此断言所有 DO 事件由同一种机制产生。线性漏水和高低阈值开关是概念化规则。

可在“与气候问题的联系”中用很短的一段说明：若观测到事件前蓄热，还要问积累是否会自行越过门槛，还是会先达到平衡。慢 - 快形状本身不能区分自维持振荡与噪声启动的事件。

参考阅读放在页末，不要占据主体验：

- Vettoretti & Peltier (2018), Fast Physics and Slow Physics in the Nonlinear Dansgaard-Oeschger Relaxation Oscillation. https://doi.org/10.1175/JCLI-D-17-0559.1
- Riechers, Gottwald & Boers (2024), Glacial Abrupt Climate Change as a Multiscale Phenomenon Resulting from Monostable Excitable Dynamics. https://doi.org/10.1175/JCLI-D-23-0308.1；作者稿 https://arxiv.org/abs/2303.04063
- Vettoretti et al. (2022), Atmospheric CO2 control of spontaneous millennial-scale ice age climate oscillations. https://doi.org/10.1038/s41561-022-00920-7

### 九、体验、性能与可访问性

- 默认中文；所有按钮与参数均用普通用户能理解的词，公式和实现细节放在折叠说明。
- 控件采用原生 button、input、select，正确关联 label；键盘可操作，有可见焦点。
- 重要状态和当前水位必须在 DOM 中有文字表达，不能只存在于 WebGL 画布。
- 适配 360px 手机宽度、768px 平板和桌面；无横向溢出，移动端保留两桶的可比较性。
- 桶标识、线型和文字与颜色共同编码。对比清楚，正文大小足以阅读。
- `prefers-reduced-motion` 下关闭装饰性波纹/飞溅，并初始暂停，提供显式“开始演示”；保留数值模拟与图表功能。
- WebGL 不可用时，显示明确说明和简化的 2D 桶/图表后备，使核心实验仍可操作，不要空白屏。
- 限制 devicePixelRatio，例如最多 2；使用 ResizeObserver 更新尺寸和相机，避免拉伸。
- 水滴/粒子数量有上限，复用几何体和材质；无逐帧泄漏。图表不必按物理步逐点重建 DOM。
- 短暂快排阶段可用 0.25× 慢放观察；不要为了延长动画而让水位和真实引擎失去同步。

### 十、验证与验收

请编写少量真正验证机制的自动化测试，而不是只测试对象字段存在。可使用 Vitest；完成 TypeScript 检查、构建和浏览器功能验证。

最低必要验证：

1. A 桶默认参数、a=0，从 hReset 开始会持续循环。解析蓄水时间为 `ln((r/k-hReset)/(r/k-H))/k`；快排时间为 `ln((H-r/(k+kappa))/(hReset-r/(k+kappa)))/(k+kappa)`。总周期约 6.636 秒，数值结果应接近。初始 h=0.70 时第一次事件不需要等一个完整周期。
2. B 桶 `r=.144,H=1,a=0` 从 h=.20 模拟 120 秒，无排水，水位趋于 .90。
3. B 桶关闭噪声、H=.75，会自行循环；关闭噪声、H=1、r=.20，也会自行循环。
4. A 桶 H 升至 1.65、a=0，当前排水结束后不会再自行启动，最终趋向 1.50。
5. 同一次 `+0.15` 加水，在 h=.88,H=1 时触发，在 h=.40,H=1 时不触发；输入账目正确增加 .15。
6. 相同 seed/初态/参数/操作模拟时间生成相同轨迹；30、60、120 FPS 的渲染调用方式不改变固定物理步结果。装饰或图表开关不影响随机轨迹。
7. `r/k==H` 的无噪声边界与有界噪声无法越过的情况，不产生由浮点舍入或展示计时器伪造的事件。
8. 关闭噪声保留当前 h 和 phase；冻结背景不暂停模拟时间；暂停不悄悄推进 OU；大阀按低复位线关闭。
9. 水量无负值，长期收支残差在合理浮点误差内，重置以外无瞬时消失。
10. 生产构建预览中：四个实验、滑块、加水、暂停、冻结背景、关闭噪声、seed 重放均工作；水位、曲线和阀门一致；手机宽度无遮挡/溢出；无控制台错误与资源 404。

浏览器可用时实际打开生产预览检查，不能仅凭构建成功宣称交互已验证。若环境缺少浏览器，完成可运行的模型测试和构建，并准确说明该项尚未验证。

### 十一、README 和 GitHub + Cloudflare Pages 交付

README 用中文写清：

- 网站用途、四个实验怎样操作。
- 模型方程、阈值状态机、噪声含义、默认值、seed 重放规则与概念模型边界。
- 与当前依赖兼容的 Node.js 版本，在项目中记录；先检查 Vite 的实际 engines 要求，不要写未经核验的旧版本。
- 本地安装 `npm ci`，开发 `npm run dev`，测试命令，`npm run build`，`npm run preview`。
- 将当前项目推送至 GitHub 后，使用 **Cloudflare Pages 的 Git 仓库集成**导入；本方案的 GitHub 用于保存源码，托管由 Cloudflare Pages 负责。
- 项目根目录为仓库根，构建命令 `npm run build`，输出目录 `dist`；生产分支由用户选择实际分支。根路径部署的 Vite base 保持 `/`。
- 若 Cloudflare 界面没有适用的预设，可手动填写上述构建命令和目录。不要因框架指南标题里含旧版 Vite 而安装旧版本。
- node_modules 不进入 Git；提交源码、package.json、lockfile 和构建配置；由 Cloudflare 构建静态产物。
- 页面不需要服务端环境变量和密钥，构建成功后是普通静态资源。

实施时遇到 Three.js API 或托管设置不确定，查当前官方资料：

- Three.js 文档：https://threejs.org/docs/
- Vite 静态部署：https://vite.dev/guide/static-deploy.html
- Cloudflare Pages 构建配置：https://developers.cloudflare.com/pages/configuration/build-configuration/
- Cloudflare Pages 的 Vite 构建与 Git 导入示例：https://developers.cloudflare.com/pages/framework-guides/deploy-a-vite3-project/

最后请给出：实现了什么、如何在本地启动、哪些验证实际通过、Cloudflare Pages 应填写的构建命令和输出目录，以及任何真实剩余限制。保持简洁，完整项目留在当前工作区。

## 提示词结束
